[#pulsarConfigureLogging]
= Configure logging for Pulsar Connector
:imagesdir: _images

Configure logging for DataStax Apache Pulsar Connector.

There are two ways to configure logging for DataStax Apache Pulsar™ Connector.

. Add an entry for `com.datastax.oss.pulsar.sink` loggers in etc/pulsar/connect-log4j.properties.
This file is used automatically by Pulsar Connector at startup.
For example, if you want to set the log level for this connector to `DEBUG`, you can add the following entry in the properties file:
+
[source,no-highlight]
----
com.datastax.oss.pulsar.sink=DEBUG
----
+
You can configure the setting to operate at lower granularity level.
Example:
+
[source,no-highlight]
----
com.datastax.oss.pulsar.sink.CassandraSinkTask=ERROR
----

. Or dynamically configure the loggers of your Pulsar Connector.
Once your Pulsar Connect API is running, retrieve all dynamic logger configurations using a `curl` command.
(These examples specify `localhost`;
provide the address of the running connect worker.)
+
[source,language-bash]
----
curl -s http://localhost:8083/admin/loggers/ | jq
----
+
To dynamically set an `ERROR` log level for `com.datastax.oss.pulsar.sink`, submit a `PUT` request via `curl`:
+
[source,language-bash]
----
curl -s -X PUT -H "Content-Type:application/json" \
      http://localhost:8083/admin/loggers/com.datastax.oss.pulsar.sink \
      -d '{"level": "ERROR"}' \
      | jq '.'
----

When you retrieve loggers again, as in the following example, upper and lower level granularity loggers are set:

[source,language-bash]
----
curl -s http://localhost:8083/admin/loggers/com.datastax.oss.pulsar.sink | jq
----

[source,results]
----
  "com.datastax.oss.pulsar.sink": {
      "level": "ERROR"
  },
  "com.datastax.oss.pulsar.sink.CassandraSinkTask": {
      "level": "ERROR"
  },
----

Again, you can set log level dynamically at a lower granularity level, such as `WARN`.
Example:

[source,language-bash]
----
curl -s -X PUT -H "Content-Type:application/json" \
  http://localhost:8083/admin/loggers/com.datastax.oss.pulsar.sink.CassandraSinkTask \
  -d '{"level": "WARN"}' \
  | jq '.'
----
